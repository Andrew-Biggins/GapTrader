<UserControl x:Class="GapAnalyserWPF.UserControls.LoadingBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GapAnalyserWPF.UserControls"
             xmlns:core="clr-namespace:GapAnalyser;assembly=GapAnalyser"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance {x:Type core:LoadingBar}}">
    <Grid>
        <ProgressBar HorizontalAlignment="Stretch" VerticalAlignment="Top" 
                     Height="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type Window}}, Path=Height}"
                     Maximum="{Binding Maximum}" Minimum="0" Value="{Binding Progress}"/>
        <TextBlock FontSize="15" Text="{Binding PercentProgress, StringFormat=P}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
    </Grid>
</UserControl>
